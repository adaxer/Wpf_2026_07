<UserControl x:Class="DocuMan.UI.Wpf.Views.ModuleHostView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:pdf="http://adaxer.de/pdf/wpf"
             xmlns:m="clr-namespace:DocuMan.Domain.Models;assembly=DocuMan.Domain.Models"
             mc:Ignorable="d" 
             d:DesignHeight="27.809" d:DesignWidth="822.753">

    <UserControl.Resources>
        <DataTemplate DataType="{x:Type m:PdfDocument}">
            <pdf:PdfView PdfBytes="{Binding Bytes}" />
        </DataTemplate>
    </UserControl.Resources>
    
    <ContentControl Content="{Binding Content}"/>
    
</UserControl>
